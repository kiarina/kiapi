type ModelName = str
"""The public ``model`` selector — a *variant within a family* (e.g. acestep
``xl-base`` / ``turbo``). Named by the variant alone; it must not repeat the
family. Used in a request's ``model`` field and as a registry lookup key."""

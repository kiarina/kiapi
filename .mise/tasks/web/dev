#!/usr/bin/env bash
#MISE description="Run the web UI dev server against a running kiapi (KIAPI_URL, default http://127.0.0.1:8000)"

set -euo pipefail

pnpm --dir web install --frozen-lockfile
pnpm --dir web dev
